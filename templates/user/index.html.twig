{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('index') }}">/pehape</a></li>
            <li class="breadcrumb-item active">Korisnici</li>
        </ol>
    </nav>

    <h1>Korisnici</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Korisničko ime</th>
                <th>Razina pristupa</th>
                <th>akcije</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.username }}</td>
                    <td>{{ 'ROLE_SUPERADMIN' in user.roles ? 'Superadmin' : 'ROLE_ADMIN' in user.roles ? 'Admin' : 'Korisnik' }}</td>
                    <td>
                        <a href="{{ path('user_edit', {'id': user.id}) }}">izmjena</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">nema korisnika</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

{% endblock %}
